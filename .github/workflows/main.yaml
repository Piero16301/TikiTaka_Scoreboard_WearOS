name: tiki_taka_scoreboard_wearos_main
# CI workflow for Tiki Taka Scoreboard WearOS application.
# This workflow checks the commit author, runs semantic checks, spell checks,
# executes tests, builds the application, and deploys it to the Play Store
# on the 'main' branch. The application is promoted from the Wear OS beta track
# to the production track.

concurrency:
  group: main-$-$
  cancel-in-progress: true

on:
  push:
    branches:
      - main

jobs:
  check-commit-type:
    name: "🔍 Check Commit Type"
    runs-on: ubuntu-latest
    outputs:
      is_dev_merge: ${{ steps.check_commit.outputs.is_dev_merge }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Check if commit is merge from dev
        id: check_commit
        run: |
          COMMIT_MSG=$(git log -1 --pretty=format:'%s')
          echo "Commit message: $COMMIT_MSG"
          if [[ "$COMMIT_MSG" == "Merge pull request"* && "$COMMIT_MSG" == *"from Piero16301/dev" ]]; then
            echo "is_dev_merge=true" >> $GITHUB_OUTPUT
            echo "Commit is a merge from dev branch, continuing with workflow"
          else
            echo "is_dev_merge=false" >> $GITHUB_OUTPUT
            echo "Commit is not a merge from dev branch, skipping workflow"
          fi

  check-commit-author:
    name: "👤 Check Commit Author"
    runs-on: ubuntu-latest
    outputs:
      is_bot: ${{ steps.check_author.outputs.is_bot }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Check commit author
        id: check_author
        run: |
          AUTHOR=$(git log -1 --pretty=format:'%an')
          echo "Commit author: $AUTHOR"
          if [[ "$AUTHOR" == "github-actions[bot]" ]]; then
            echo "is_bot=true" >> $GITHUB_OUTPUT
            echo "Commit was made by github-actions[bot], skipping jobs"
          else
            echo "is_bot=false" >> $GITHUB_OUTPUT
            echo "Commit was made by a user, continuing"
          fi

  semantic-pull-request:
    name: "📝 Semantic"
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/semantic_pull_request.yml@v1
    permissions:
      contents: read

  spell-check:
    name: "🔍 Spell Check"
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/spell_check.yml@v1
    with:
      includes: |
        **/*.md
      modified_files_only: false

  tests:
    name: "🧪 Run Tests"
    needs: [semantic-pull-request, spell-check]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📚 Git Checkout
        uses: actions/checkout@v4
      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
      - name: 📦 Install Dependencies
        run: |
          flutter pub global activate very_good_cli
          very_good packages get --recursive
      - name: 🧪 Run Tests
        run: very_good test --coverage
      - name: 📊 Generate Coverage Badge
        run: |
          dart run test_coverage_badge
      - name: 📊 Upload Coverage Report
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet coverage_badge.svg; then
            echo "No changes to coverage badge, skipping commit"
          else
            git add coverage_badge.svg
            git commit -m "Update coverage badge [skip ci]"
            git push origin main
          fi

  promote:
    name: "📈 Promote to Production"
    needs: tests
    runs-on: ubuntu-latest
    env:
      APP_PACKAGE_NAME: com.pmorales.wearos.tikitaka
      SERVICE_ACCOUNT_PATH: service-account.json
    steps:
      - name: 📚 Git Checkout
        uses: actions/checkout@v4
      - name: 🔑 Decode Service Account
        run: echo "${{ secrets.ANDROID_RELEASE_SERVICE_ACCOUNT }}" | base64 --decode > ${{ env.SERVICE_ACCOUNT_PATH }}
      - name: 🚀 Promote to Production
        uses: kevin-david/promote-play-release@v1.2.0
        with:
          service-account-json-file: ${{ env.SERVICE_ACCOUNT_PATH }}
          package-name: ${{ env.APP_PACKAGE_NAME }}
          from-track: 'wear:beta'
          to-track: 'wear:production'
